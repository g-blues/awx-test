---
- hosts: server
#  gather_facts: false
#  connection: local
  
  tasks:
  - name: ping test
    command: "ping -c 3 192.168.0.1"
    register: result
  - name: set_fact
    set_fact:
      test_fact: "TESTTEST"
  - name: debug
    debug:
      msg: "{{ result.stdout }}"
      
